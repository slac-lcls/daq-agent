# Deployment assets

This directory is reserved for reviewed service and scheduler templates once
the reporting runtime works. There is no running or deployable service yet.

See [deployment design](../docs/deployment.md) for ownership, credential, storage,
preflight, and scheduling requirements. Templates must reference external
credentials rather than contain secret values.
